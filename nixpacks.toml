[phases.setup]
nixPkgs = [
  "php83",
  "php83Packages.composer",
  "python311"
]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "pip install -r requirements.txt"
]
